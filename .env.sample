# ==========================================
# CONFIGURATION FOR THE FRONTEND (Vite)
# ==========================================
# URL where the browser will look for the backend. 
# If testing on your PC: http://localhost:5000
# If accessing from local network: http://<SERVER_IP>:5000
VITE_BACKEND_URL=http://localhost:5000

# ==========================================
# CONFIGURATION FOR THE SERVER (Backend)
# ==========================================
# Allowed origins (CORS). Must match your browser's URL.
# Separate with commas if there are multiple (e.g., http://localhost:3000,http://192.168.1.10:3000)
CLIENT_URLS=http://localhost:3000

# Internal port where the server runs
PORT=5000
NODE_ENV=dev

# GitHub integration (Required to save diagrams)
# Create one at: GitHub Settings -> Developer settings -> Personal access tokens (classic)
# Required permissions: 'repo' and 'gist'
GITHUB_TOKEN=your_token_here

# Email configuration (Optional - For notifications with other users)
MAIL_SERVICE=gmail
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-google-app-password